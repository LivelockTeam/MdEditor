name: Swift
on:
  pull_request:
    branches: [ "mdeditor-my-beta-version" ]
jobs:
  build:
    name: Build App
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3
      - run: exec ./.github/build.sh
